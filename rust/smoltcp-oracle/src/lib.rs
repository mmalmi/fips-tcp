//! Test-only smoltcp interoperability oracle for TCP/FIPS.
//!
//! This crate is not published and has no production runtime API. Its
//! integration tests keep synthetic IP and checksum adaptation outside the
//! public `fips-tcp` crates.
