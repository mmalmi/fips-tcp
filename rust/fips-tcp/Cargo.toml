[package]
name = "nvpn-fips-tcp"
version = "0.2.1"
description = "TCP reliable streams over authenticated FIPS datagrams"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[lib]
name = "fips_tcp"

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
